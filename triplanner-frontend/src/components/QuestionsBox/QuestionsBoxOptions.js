import React from "react";
import styles from "../Reviews/Reviews.module.css";

const QuestionsBoxOptions = () => {
    return(
        <div>
            <button className={styles.reviews_addBtn}>Add Question</button>
        </div>
    );
};

export default QuestionsBoxOptions;